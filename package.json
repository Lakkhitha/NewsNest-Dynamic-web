{
  "name": "newsnest",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "lint:fix": "npm run lint:fix --workspace client && npm run lint:fix --workspace server",
    "type-check": "npm run type-check --workspace client && npm run type-check --workspace server",
    "format": "npm run format --workspace client",
    "test": "npm run test --workspace client && npm run test --workspace server",
    "clean": "npm run clean --workspace client && npm run clean --workspace server",
    "docker:build": "docker build -t newsnest .",
    "docker:run": "docker run -p 4000:4000 newsnest",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
